import { Address } from '@ton/ton';
import { AddressType } from '../types.cjs';

/** Convert passed value to Address instance if it's not already */
declare function toAddress(addressValue: AddressType): Address;

export { toAddress };
