import { Contract as Contract$1, Address } from '@ton/ton';
import { AddressType } from '../../types.cjs';

interface ContractOptions {
}
declare abstract class Contract implements Contract$1 {
    readonly address: Address;
    constructor(address: AddressType, options?: ContractOptions);
    static create<T extends Contract, C extends new (address: AddressType) => T>(this: C, address: AddressType): InstanceType<C>;
}

export { Contract, type ContractOptions };
