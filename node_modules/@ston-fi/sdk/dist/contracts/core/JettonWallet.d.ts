import * as _ton_ton from '@ton/ton';
import { ContractProvider } from '@ton/ton';
import { Contract } from './Contract.js';
import '../../types.js';

declare class JettonWallet extends Contract {
    getBalance(provider: ContractProvider): Promise<bigint>;
    getWalletData(provider: ContractProvider): Promise<{
        balance: bigint;
        ownerAddress: _ton_ton.Address;
        jettonMasterAddress: _ton_ton.Address;
        jettonWalletCode: _ton_ton.Cell;
    }>;
}

export { JettonWallet };
